export interface NoteResponse {
    id: number;
    description: string;
    userId: number;
    createdAt: Date;
    updatedAt: Date;
}

    // export interface RootObject {
    //     notes: Note[];
    // }
